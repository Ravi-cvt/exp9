version: '3.8'
services:
  client:
    build: ./client
    ports:
      - "3000:3000"
  server:
    build: ./server
    ports:
      - "3001:3001"
# Note: Docker for local dev only; Vercel uses serverless

