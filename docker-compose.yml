version: '3.8'
services:
  backend-app:
    build: ./server
    ports:
      - "3001:3001"
  frontend-app:
    build: ./client
    ports:
      - "3000:3000"
    depends_on:
      - backend-app
# Local dev; CI/CD pushes to Docker Hub
